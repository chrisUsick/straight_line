require 'spec_helper'
require 'common/feature/diff'

describe Feature::Land do
  it 'does not land if uncommitted files exist'

  it 'merges master branch to feature branch'

  it 'merges feature branch to master'

  it 'does not merge feature branch to master if done in github'

  it 'deletes feature branch'
end
